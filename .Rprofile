source("renv/activate.R")
# The setenvs actually break things if the .venv is in the outer Rproject directory and we don't feed it a full path. Python seems to just be found if they share the dir
# Sys.setenv(RETICULATE_PYTHON = ".venv/Scripts/python.exe")
# Sys.setenv(RETICULATE_PYTHON = 'C:/Users/galen/Documents/HydroBOT_website/.venv/Scripts/python.exe')

